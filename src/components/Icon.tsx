import React from "react";
interface Props {
  icon: string;
  fixedWidth?: boolean;
  spin?: boolean;
  transform?: string;
}

function Icon({ icon, ...rest }: Props) {
  // temporary
  return null;
}

export default Icon;
